spring:
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: ${spring.cloud.nacos.namespace}
        file-extension: yaml
        prefix: datasource-dev
        group: KEANN_DATASOURCE